package com.nutrino.timbreassignment.ui.theme

import androidx.compose.ui.graphics.Color

/** Solid black color token used for app background and dark surfaces. */
val AppBlack = Color(0xFF000000)

/** Solid white color token used for text and iconography on dark background. */
val AppWhite = Color(0xFFFFFFFF)

/** Crimson red accent color token used for primary action items, highlights, and borders. */
val CrimsonAccent = Color(0xFFD50000)
